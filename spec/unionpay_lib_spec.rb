require 'spec_helper'

describe UnionpayLib do
  it 'has a version number' do
    expect(UnionpayLib::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
